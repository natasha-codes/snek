name: Shellcheck

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Shellcheck
        working-directory: ./bin
        run: ./shellcheck.sh
      - name: Test
        working-directory: ./
        run: ls && pwd
      - name: Test II
        run: ls && pwd
